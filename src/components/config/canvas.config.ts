export const ARTBOARD_WIDTH = 1200;
export const ARTBOARD_HEIGHT = 800;
export const RULER_SIZE = 30;
export const VIRTUAL_SIZE = 8000; // total virtual workspace (allows negative space)
export const CANVAS_OFFSET = VIRTUAL_SIZE / 2; // offset to place origin at center
export const GRID_STEP = 50;
